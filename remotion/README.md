# Video landing · Perkins

Composición Remotion `PerkinsHowItWorks` (1280×720, 15 s, 30 fps): intro + 3 pasos + cierre.

## Comandos (desde la raíz del repo)

```bash
npm run remotion:studio   # previsualizar y editar
npm run remotion:render   # exporta a public/videos/perkins-como-funciona.mp4
```

Tras cambiar la composición, vuelve a ejecutar `remotion:render` antes de deploy.
